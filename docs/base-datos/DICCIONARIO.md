# Diccionario de la base compatible optimizada

Catalogo real: 2026-09-09T20:14:15.299Z. 34 tablas y 236 columnas. Solo objetos conservados.

Tipos, nulabilidad, valores por defecto y restricciones se preservan del origen; no se endurecen reglas por comparacion con SQL historicos.

La evidencia por tabla enumera referencias lexicas para facilitar la revision; la clasificacion funcional esta en INFORME.md.

## public.comunidad_comentario_likes

Filas observadas: 1. RLS: activado.

Referencias: `Backend/Controllers/comunidadController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| user_id | uuid | No | — |
| comentario_id | bigint | No | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| comunidad_comentario_likes_comentario_id_fkey | FOREIGN KEY (comentario_id) REFERENCES comunidad_comentarios(id) ON DELETE CASCADE |
| comunidad_comentario_likes_pkey | PRIMARY KEY (user_id, comentario_id) |
| comunidad_comentario_likes_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| comunidad_comentario_likes_pkey | CREATE UNIQUE INDEX comunidad_comentario_likes_pkey ON public.comunidad_comentario_likes USING btree (user_id, comentario_id) |
| idx_comunidad_comentario_likes_comentario | CREATE INDEX idx_comunidad_comentario_likes_comentario ON public.comunidad_comentario_likes USING btree (comentario_id) |

## public.comunidad_comentarios

Filas observadas: 18. RLS: activado.

Referencias: `Backend/Controllers/comunidadController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | bigint | No | GENERATED BY DEFAULT AS IDENTITY |
| publicacion_id | bigint | No | — |
| user_id | uuid | No | — |
| parent_id | bigint | Si | — |
| texto | text | No | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |
| likes | integer | No | 0 |
| responde_a | text | Si | — |

| Restriccion | Definicion |
|---|---|
| comunidad_comentarios_parent_id_fkey | FOREIGN KEY (parent_id) REFERENCES comunidad_comentarios(id) ON DELETE CASCADE |
| comunidad_comentarios_pkey | PRIMARY KEY (id) |
| comunidad_comentarios_publicacion_id_fkey | FOREIGN KEY (publicacion_id) REFERENCES comunidad_publicaciones(id) ON DELETE CASCADE |
| comunidad_comentarios_texto_check | CHECK ((length(TRIM(BOTH FROM texto)) > 0)) |
| comunidad_comentarios_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| comunidad_comentarios_pkey | CREATE UNIQUE INDEX comunidad_comentarios_pkey ON public.comunidad_comentarios USING btree (id) |
| idx_comunidad_comentarios_parent | CREATE INDEX idx_comunidad_comentarios_parent ON public.comunidad_comentarios USING btree (parent_id) |
| idx_comunidad_comentarios_publicacion | CREATE INDEX idx_comunidad_comentarios_publicacion ON public.comunidad_comentarios USING btree (publicacion_id, created_at) |

## public.comunidad_miembros

Filas observadas: 3. RLS: activado.

Referencias: `Backend/Controllers/comunidadController.js`, `Backend/Controllers/usuarioController.js`, `Backend/services/notificationService.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| comunidad_id | text | No | — |
| user_id | uuid | No | — |
| nivel_notificaciones | text | No | 'todas'::text |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| comunidad_miembros_comunidad_id_fkey | FOREIGN KEY (comunidad_id) REFERENCES comunidades(id) ON DELETE CASCADE |
| comunidad_miembros_nivel_notificaciones_check | CHECK ((nivel_notificaciones = ANY (ARRAY['todas'::text, 'relevantes'::text, 'silenciadas'::text]))) |
| comunidad_miembros_pkey | PRIMARY KEY (comunidad_id, user_id) |
| comunidad_miembros_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| comunidad_miembros_pkey | CREATE UNIQUE INDEX comunidad_miembros_pkey ON public.comunidad_miembros USING btree (comunidad_id, user_id) |
| idx_comunidad_miembros_user | CREATE INDEX idx_comunidad_miembros_user ON public.comunidad_miembros USING btree (user_id, created_at DESC) |

## public.comunidad_publicacion_guardados

Filas observadas: 3. RLS: activado.

Referencias: `Backend/Controllers/comunidadController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| user_id | uuid | No | — |
| publicacion_id | bigint | No | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| comunidad_publicacion_guardados_pkey | PRIMARY KEY (user_id, publicacion_id) |
| comunidad_publicacion_guardados_publicacion_id_fkey | FOREIGN KEY (publicacion_id) REFERENCES comunidad_publicaciones(id) ON DELETE CASCADE |
| comunidad_publicacion_guardados_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| comunidad_publicacion_guardados_pkey | CREATE UNIQUE INDEX comunidad_publicacion_guardados_pkey ON public.comunidad_publicacion_guardados USING btree (user_id, publicacion_id) |
| idx_comunidad_publicacion_guardados_publicacion | CREATE INDEX idx_comunidad_publicacion_guardados_publicacion ON public.comunidad_publicacion_guardados USING btree (publicacion_id) |

## public.comunidad_publicacion_likes

Filas observadas: 3. RLS: activado.

Referencias: `Backend/Controllers/comunidadController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| user_id | uuid | No | — |
| publicacion_id | bigint | No | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| comunidad_publicacion_likes_pkey | PRIMARY KEY (user_id, publicacion_id) |
| comunidad_publicacion_likes_publicacion_id_fkey | FOREIGN KEY (publicacion_id) REFERENCES comunidad_publicaciones(id) ON DELETE CASCADE |
| comunidad_publicacion_likes_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| comunidad_publicacion_likes_pkey | CREATE UNIQUE INDEX comunidad_publicacion_likes_pkey ON public.comunidad_publicacion_likes USING btree (user_id, publicacion_id) |
| idx_comunidad_publicacion_likes_publicacion | CREATE INDEX idx_comunidad_publicacion_likes_publicacion ON public.comunidad_publicacion_likes USING btree (publicacion_id) |

## public.comunidad_publicaciones

Filas observadas: 19. RLS: activado.

Referencias: `Backend/Controllers/comunidadController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | bigint | No | GENERATED BY DEFAULT AS IDENTITY |
| comunidad_id | text | No | — |
| user_id | uuid | No | — |
| tipo | text | No | 'reciente'::text |
| titulo | text | No | — |
| texto | text | No | — |
| etiqueta | text | Si | — |
| fijada | boolean | No | false |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |
| evento_asociado_id | text | Si | — |
| reel_asociado_id | text | Si | — |
| likes | integer | No | 0 |
| guardados | integer | No | 0 |
| updated_at | timestamp with time zone | Si | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| comunidad_publicaciones_comunidad_id_fkey | FOREIGN KEY (comunidad_id) REFERENCES comunidades(id) ON DELETE CASCADE |
| comunidad_publicaciones_pkey | PRIMARY KEY (id) |
| comunidad_publicaciones_texto_check | CHECK ((length(TRIM(BOTH FROM texto)) > 0)) |
| comunidad_publicaciones_tipo_check | CHECK ((tipo = ANY (ARRAY['destacado'::text, 'reciente'::text, 'popular'::text, 'preguntas'::text]))) |
| comunidad_publicaciones_titulo_check | CHECK ((length(TRIM(BOTH FROM titulo)) > 0)) |
| comunidad_publicaciones_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| comunidad_publicaciones_pkey | CREATE UNIQUE INDEX comunidad_publicaciones_pkey ON public.comunidad_publicaciones USING btree (id) |
| idx_comunidad_publicaciones_comunidad | CREATE INDEX idx_comunidad_publicaciones_comunidad ON public.comunidad_publicaciones USING btree (comunidad_id, created_at DESC) |
| idx_comunidad_publicaciones_user | CREATE INDEX idx_comunidad_publicaciones_user ON public.comunidad_publicaciones USING btree (user_id) |

## public.comunidades

Filas observadas: 13. RLS: activado.

Referencias: `Backend/Controllers/comunidadController.js`, `Frontend/src/paginas/Auth.jsx`, `Frontend/src/paginas/Comunidad.jsx`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | text | No | — |
| nombre | text | No | — |
| titulo | text | No | — |
| genero | text | No | — |
| descripcion | text | No | — |
| portada_url | text | Si | — |
| activa | boolean | No | true |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| comunidades_genero_key | UNIQUE (genero) |
| comunidades_nombre_key | UNIQUE (nombre) |
| comunidades_pkey | PRIMARY KEY (id) |

| Indice | Definicion |
|---|---|
| comunidades_genero_key | CREATE UNIQUE INDEX comunidades_genero_key ON public.comunidades USING btree (genero) |
| comunidades_nombre_key | CREATE UNIQUE INDEX comunidades_nombre_key ON public.comunidades USING btree (nombre) |
| comunidades_pkey | CREATE UNIQUE INDEX comunidades_pkey ON public.comunidades USING btree (id) |

## public.content_reports

Filas observadas: 0. RLS: activado.

Referencias: `Backend/services/moderationService.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | bigint | No | GENERATED BY DEFAULT AS IDENTITY |
| reporter_id | uuid | No | — |
| reported_user_id | uuid | Si | — |
| content_type | text | No | — |
| content_id | text | No | — |
| reason | text | No | 'contenido_inapropiado'::text |
| status | text | No | 'pendiente'::text |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |
| details | text | No | ''::text |

| Restriccion | Definicion |
|---|---|
| content_reports_content_type_check | CHECK ((content_type = ANY (ARRAY['reel'::text, 'evento'::text, 'perfil'::text, 'community_post'::text, 'community_comment'::text, 'profile_post'::text, 'profile_post_reply'::text]))) |
| content_reports_pkey | PRIMARY KEY (id) |
| content_reports_reported_user_id_fkey | FOREIGN KEY (reported_user_id) REFERENCES users(id) ON DELETE SET NULL |
| content_reports_reporter_id_fkey | FOREIGN KEY (reporter_id) REFERENCES users(id) ON DELETE CASCADE |
| content_reports_unique | UNIQUE (reporter_id, content_type, content_id) |

| Indice | Definicion |
|---|---|
| content_reports_pkey | CREATE UNIQUE INDEX content_reports_pkey ON public.content_reports USING btree (id) |
| content_reports_unique | CREATE UNIQUE INDEX content_reports_unique ON public.content_reports USING btree (reporter_id, content_type, content_id) |
| idx_content_reports_status | CREATE INDEX idx_content_reports_status ON public.content_reports USING btree (status, created_at DESC) |

## public.conversation_members

Filas observadas: 4. RLS: activado.

Referencias: `Backend/Controllers/mensajeController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| conversation_id | uuid | No | — |
| user_id | uuid | No | — |
| joined_at | timestamp with time zone | No | timezone('utc'::text, now()) |
| last_read_at | timestamp with time zone | No | timezone('utc'::text, now()) |
| muted | boolean | No | false |
| archived_at | timestamp with time zone | Si | — |
| last_delivered_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| conversation_members_conversation_id_fkey | FOREIGN KEY (conversation_id) REFERENCES conversations(id) ON DELETE CASCADE |
| conversation_members_pkey | PRIMARY KEY (conversation_id, user_id) |

| Indice | Definicion |
|---|---|
| conversation_members_pkey | CREATE UNIQUE INDEX conversation_members_pkey ON public.conversation_members USING btree (conversation_id, user_id) |
| conversation_members_user_id_idx | CREATE INDEX conversation_members_user_id_idx ON public.conversation_members USING btree (user_id) |
| conversation_members_user_idx | CREATE INDEX conversation_members_user_idx ON public.conversation_members USING btree (user_id, conversation_id) |

## public.conversations

Filas observadas: 78. RLS: activado.

Referencias: `Backend/Controllers/mensajeController.js`, `Frontend/src/contextos/PreferenciasContext.jsx`, `Frontend/src/paginas/Mensajes.jsx`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | uuid | No | gen_random_uuid() |
| kind | text | No | 'direct'::text |
| direct_key | text | No | — |
| created_by | uuid | Si | — |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |
| updated_at | timestamp with time zone | No | timezone('utc'::text, now()) |
| last_message_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| conversations_created_by_fkey | FOREIGN KEY (created_by) REFERENCES users(id) ON DELETE SET NULL |
| conversations_direct_key_key | UNIQUE (direct_key) |
| conversations_kind_check | CHECK ((kind = 'direct'::text)) |
| conversations_pkey | PRIMARY KEY (id) |

| Indice | Definicion |
|---|---|
| conversations_direct_key_key | CREATE UNIQUE INDEX conversations_direct_key_key ON public.conversations USING btree (direct_key) |
| conversations_last_message_idx | CREATE INDEX conversations_last_message_idx ON public.conversations USING btree (last_message_at DESC, id) |
| conversations_pkey | CREATE UNIQUE INDEX conversations_pkey ON public.conversations USING btree (id) |
| conversations_updated_idx | CREATE INDEX conversations_updated_idx ON public.conversations USING btree (updated_at DESC) |

## public.event_organizers

Filas observadas: 2. RLS: activado.

Referencias: `Backend/Controllers/eventoController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| event_id | bigint | No | — |
| user_id | uuid | No | — |
| added_by | uuid | No | — |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| event_organizers_added_by_fkey | FOREIGN KEY (added_by) REFERENCES users(id) ON DELETE CASCADE |
| event_organizers_event_id_fkey | FOREIGN KEY (event_id) REFERENCES eventos(id) ON DELETE CASCADE |
| event_organizers_pkey | PRIMARY KEY (event_id, user_id) |
| event_organizers_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| event_organizers_pkey | CREATE UNIQUE INDEX event_organizers_pkey ON public.event_organizers USING btree (event_id, user_id) |
| event_organizers_user_id_idx | CREATE INDEX event_organizers_user_id_idx ON public.event_organizers USING btree (user_id) |

## public.event_saves

Filas observadas: 2. RLS: activado.

Referencias: `Backend/Controllers/eventoController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| user_id | uuid | No | — |
| event_id | bigint | No | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| event_saves_event_id_fkey | FOREIGN KEY (event_id) REFERENCES eventos(id) ON DELETE CASCADE |
| event_saves_pkey | PRIMARY KEY (user_id, event_id) |
| event_saves_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| event_saves_pkey | CREATE UNIQUE INDEX event_saves_pkey ON public.event_saves USING btree (user_id, event_id) |
| idx_event_saves_event_id | CREATE INDEX idx_event_saves_event_id ON public.event_saves USING btree (event_id) |

## public.evento_generos

Filas observadas: 4. RLS: activado.

Referencias: `Backend/Controllers/eventoController.js`, `Backend/Controllers/perfilComunidadController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| event_id | bigint | No | — |
| genero | text | No | — |
| posicion | smallint | No | — |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| evento_generos_event_id_fkey | FOREIGN KEY (event_id) REFERENCES eventos(id) ON DELETE CASCADE |
| evento_generos_event_id_posicion_key | UNIQUE (event_id, posicion) |
| evento_generos_genero_fkey | FOREIGN KEY (genero) REFERENCES generos(slug) |
| evento_generos_pkey | PRIMARY KEY (event_id, genero) |
| evento_generos_posicion_check | CHECK (((posicion >= 1) AND (posicion <= 3))) |

| Indice | Definicion |
|---|---|
| evento_generos_event_id_posicion_key | CREATE UNIQUE INDEX evento_generos_event_id_posicion_key ON public.evento_generos USING btree (event_id, posicion) |
| evento_generos_genero_event_idx | CREATE INDEX evento_generos_genero_event_idx ON public.evento_generos USING btree (genero, event_id) |
| evento_generos_pkey | CREATE UNIQUE INDEX evento_generos_pkey ON public.evento_generos USING btree (event_id, genero) |

## public.eventos

Filas observadas: 4. RLS: activado.

Referencias: `Backend/Controllers/comunidadController.js`, `Backend/Controllers/eventoController.js`, `Backend/Controllers/perfilComunidadController.js`, `Backend/Controllers/reelController.js`, `Backend/Controllers/usuarioController.js`, `Backend/services/storageService.js`, `Frontend/src/componentes/Navbar.jsx`, `Frontend/src/componentes/SidebarNav.jsx`, `Frontend/src/contextos/PreferenciasContext.jsx`, `Frontend/src/paginas/Auth.jsx`, `Frontend/src/paginas/Buscar.jsx`, `Frontend/src/paginas/Comunidad.jsx`, `Frontend/src/paginas/Configuracion.jsx`, `Frontend/src/paginas/Eventos.jsx`, `Frontend/src/paginas/Miperfil.jsx`, `Frontend/src/paginas/OtroPerfil.jsx`, `Frontend/src/paginas/Soporte.jsx`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | bigint | No | GENERATED BY DEFAULT AS IDENTITY |
| genero | text | No | — |
| lugar | text | Si | — |
| fecha | timestamp with time zone | No | — |
| img_url | text | Si | — |
| link | text | Si | — |
| creador_id | uuid | No | — |
| latitud | double precision | Si | — |
| longitud | double precision | Si | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |
| precio | numeric(12,2) | Si | — |
| img_path | text | Si | — |
| ubicacion_geog | gis.geography(Point,4326) | Si | — |
| descripcion | text | Si | — |
| titulo | text | No | 'Evento SONDAR'::text |

| Restriccion | Definicion |
|---|---|
| eventos_creador_id_fkey | FOREIGN KEY (creador_id) REFERENCES users(id) ON DELETE CASCADE |
| eventos_pkey | PRIMARY KEY (id) |
| eventos_precio_check | CHECK (((precio IS NULL) OR (precio >= (0)::numeric))) |

| Indice | Definicion |
|---|---|
| eventos_creador_id_idx | CREATE INDEX eventos_creador_id_idx ON public.eventos USING btree (creador_id) |
| eventos_pkey | CREATE UNIQUE INDEX eventos_pkey ON public.eventos USING btree (id) |
| eventos_ubicacion_geog_gist_idx | CREATE INDEX eventos_ubicacion_geog_gist_idx ON public.eventos USING gist (ubicacion_geog) |
| idx_eventos_fecha | CREATE INDEX idx_eventos_fecha ON public.eventos USING btree (fecha) |

## public.follows

Filas observadas: 2. RLS: activado.

Referencias: `Backend/Controllers/perfilComunidadController.js`, `Backend/Controllers/reelController.js`, `Backend/Controllers/usuarioController.js`, `Backend/services/notificationService.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| follower_id | uuid | No | — |
| following_id | uuid | No | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| follows_follower_id_fkey | FOREIGN KEY (follower_id) REFERENCES users(id) ON DELETE CASCADE |
| follows_following_id_fkey | FOREIGN KEY (following_id) REFERENCES users(id) ON DELETE CASCADE |
| follows_no_self | CHECK ((follower_id <> following_id)) |
| follows_pkey | PRIMARY KEY (follower_id, following_id) |

| Indice | Definicion |
|---|---|
| follows_pkey | CREATE UNIQUE INDEX follows_pkey ON public.follows USING btree (follower_id, following_id) |
| idx_follows_following_id | CREATE INDEX idx_follows_following_id ON public.follows USING btree (following_id) |

## public.generos

Filas observadas: 14. RLS: activado.

Referencias: `Backend/Controllers/eventoController.js`, `Backend/Controllers/perfilComunidadController.js`, `Backend/Controllers/reelController.js`, `Backend/Controllers/usuarioController.js`, `Frontend/src/componentes/CrearReelModal.jsx`, `Frontend/src/componentes/OnboardingPerfilModal.jsx`, `Frontend/src/componentes/PerfilComunidad.jsx`, `Frontend/src/paginas/Buscar.jsx`, `Frontend/src/paginas/Comunidad.jsx`, `Frontend/src/paginas/Descubrir.jsx`, `Frontend/src/paginas/Eventos.jsx`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| slug | text | No | — |
| nombre | text | No | — |
| activo | boolean | No | true |
| orden | smallint | No | 100 |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| generos_nombre_check | CHECK ((length(TRIM(BOTH FROM nombre)) > 0)) |
| generos_pkey | PRIMARY KEY (slug) |
| generos_slug_check | CHECK ((length(TRIM(BOTH FROM slug)) > 0)) |

| Indice | Definicion |
|---|---|
| generos_pkey | CREATE UNIQUE INDEX generos_pkey ON public.generos USING btree (slug) |

## public.messages

Filas observadas: 7. RLS: activado.

Referencias: `Backend/Controllers/mensajeController.js`, `Frontend/src/contextos/PreferenciasContext.jsx`, `Frontend/src/paginas/Mensajes.jsx`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | bigint | No | GENERATED BY DEFAULT AS IDENTITY |
| conversation_id | uuid | No | — |
| sender_id | uuid | Si | — |
| body | text | No | — |
| reply_to_id | bigint | Si | — |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |
| edited_at | timestamp with time zone | Si | — |
| deleted_at | timestamp with time zone | Si | — |

| Restriccion | Definicion |
|---|---|
| messages_body_length_check | CHECK (((deleted_at IS NOT NULL) OR ((length(TRIM(BOTH FROM body)) >= 1) AND (length(TRIM(BOTH FROM body)) <= 2000)))) |
| messages_conversation_id_fkey | FOREIGN KEY (conversation_id) REFERENCES conversations(id) ON DELETE CASCADE |
| messages_pkey | PRIMARY KEY (id) |
| messages_reply_to_id_fkey | FOREIGN KEY (reply_to_id) REFERENCES messages(id) ON DELETE SET NULL |
| messages_sender_id_fkey | FOREIGN KEY (sender_id) REFERENCES users(id) ON DELETE SET NULL |

| Indice | Definicion |
|---|---|
| messages_conversation_created_idx | CREATE INDEX messages_conversation_created_idx ON public.messages USING btree (conversation_id, created_at, id) |
| messages_conversation_cursor_idx | CREATE INDEX messages_conversation_cursor_idx ON public.messages USING btree (conversation_id, id DESC) |
| messages_pkey | CREATE UNIQUE INDEX messages_pkey ON public.messages USING btree (id) |
| messages_sender_id_idx | CREATE INDEX messages_sender_id_idx ON public.messages USING btree (sender_id) |
| messages_sender_rate_idx | CREATE INDEX messages_sender_rate_idx ON public.messages USING btree (sender_id, created_at DESC) |

## public.notification_mutes

Filas observadas: 0. RLS: activado.

Referencias: `Backend/Controllers/usuarioController.js`, `Backend/services/notificationService.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| user_id | uuid | No | — |
| muted_user_id | uuid | No | — |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| notification_mutes_follow_fk | FOREIGN KEY (user_id, muted_user_id) REFERENCES follows(follower_id, following_id) ON DELETE CASCADE |
| notification_mutes_no_self | CHECK ((user_id <> muted_user_id)) |
| notification_mutes_pkey | PRIMARY KEY (user_id, muted_user_id) |

| Indice | Definicion |
|---|---|
| notification_mutes_muted_user_idx | CREATE INDEX notification_mutes_muted_user_idx ON public.notification_mutes USING btree (muted_user_id) |
| notification_mutes_pkey | CREATE UNIQUE INDEX notification_mutes_pkey ON public.notification_mutes USING btree (user_id, muted_user_id) |

## public.notifications

Filas observadas: 54. RLS: activado.

Referencias: `Backend/Controllers/comunidadController.js`, `Backend/Controllers/mensajeController.js`, `Backend/Controllers/notificacionController.js`, `Backend/Controllers/usuarioController.js`, `Backend/services/notificationService.js`, `Frontend/src/componentes/Navbar.jsx`, `Frontend/src/componentes/NotificationPanel.jsx`, `Frontend/src/contextos/PreferenciasContext.jsx`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | bigint | No | GENERATED BY DEFAULT AS IDENTITY |
| user_id | uuid | No | — |
| actor_id | uuid | Si | — |
| type | text | No | — |
| title | text | No | — |
| body | text | No | ''::text |
| target_url | text | No | ''::text |
| unique_key | text | Si | — |
| read_at | timestamp with time zone | Si | — |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |
| entity_type | text | Si | — |
| entity_id | text | Si | — |
| metadata | jsonb | No | '{}'::jsonb |

| Restriccion | Definicion |
|---|---|
| notifications_actor_id_fkey | FOREIGN KEY (actor_id) REFERENCES users(id) ON DELETE SET NULL |
| notifications_body_check | CHECK ((length(body) <= 500)) |
| notifications_pkey | PRIMARY KEY (id) |
| notifications_title_check | CHECK ((length(title) <= 120)) |
| notifications_unique_key_key | UNIQUE (unique_key) |
| notifications_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| notifications_pkey | CREATE UNIQUE INDEX notifications_pkey ON public.notifications USING btree (id) |
| notifications_unique_key_key | CREATE UNIQUE INDEX notifications_unique_key_key ON public.notifications USING btree (unique_key) |
| notifications_user_created_idx | CREATE INDEX notifications_user_created_idx ON public.notifications USING btree (user_id, created_at DESC) |
| notifications_user_unread_idx | CREATE INDEX notifications_user_unread_idx ON public.notifications USING btree (user_id, created_at DESC) WHERE (read_at IS NULL) |

## public.perfil_comunidad_publicaciones

Filas observadas: 7. RLS: activado.

Referencias: `Backend/Controllers/perfilComunidadController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | bigint | No | GENERATED BY DEFAULT AS IDENTITY |
| user_id | uuid | No | — |
| origen | text | No | 'manual'::text |
| texto | text | No | — |
| reel_id | bigint | Si | — |
| evento_id | bigint | Si | — |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |
| updated_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| perfil_comunidad_origen_adjunto_check | CHECK (((origen = 'manual'::text) OR ((origen = 'reel'::text) AND (reel_id IS NOT NULL) AND (evento_id IS NULL)) OR ((origen = 'evento'::text) AND (evento_id IS NOT NULL) AND (reel_id IS NULL)))) |
| perfil_comunidad_publicaciones_evento_id_fkey | FOREIGN KEY (evento_id) REFERENCES eventos(id) ON DELETE CASCADE |
| perfil_comunidad_publicaciones_origen_check | CHECK ((origen = ANY (ARRAY['manual'::text, 'reel'::text, 'evento'::text]))) |
| perfil_comunidad_publicaciones_pkey | PRIMARY KEY (id) |
| perfil_comunidad_publicaciones_reel_id_fkey | FOREIGN KEY (reel_id) REFERENCES reels(id) ON DELETE CASCADE |
| perfil_comunidad_publicaciones_texto_check | CHECK (((length(TRIM(BOTH FROM texto)) >= 1) AND (length(TRIM(BOTH FROM texto)) <= 1000))) |
| perfil_comunidad_publicaciones_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |
| perfil_comunidad_un_adjunto_check | CHECK ((num_nonnulls(reel_id, evento_id) <= 1)) |

| Indice | Definicion |
|---|---|
| perfil_comunidad_auto_evento_idx | CREATE UNIQUE INDEX perfil_comunidad_auto_evento_idx ON public.perfil_comunidad_publicaciones USING btree (evento_id) WHERE (origen = 'evento'::text) |
| perfil_comunidad_auto_reel_idx | CREATE UNIQUE INDEX perfil_comunidad_auto_reel_idx ON public.perfil_comunidad_publicaciones USING btree (reel_id) WHERE (origen = 'reel'::text) |
| perfil_comunidad_publicaciones_pkey | CREATE UNIQUE INDEX perfil_comunidad_publicaciones_pkey ON public.perfil_comunidad_publicaciones USING btree (id) |
| perfil_comunidad_publicaciones_user_idx | CREATE INDEX perfil_comunidad_publicaciones_user_idx ON public.perfil_comunidad_publicaciones USING btree (user_id, created_at DESC, id DESC) |

## public.perfil_comunidad_respuestas

Filas observadas: 0. RLS: activado.

Referencias: `Backend/Controllers/perfilComunidadController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | bigint | No | GENERATED BY DEFAULT AS IDENTITY |
| publicacion_id | bigint | No | — |
| user_id | uuid | No | — |
| parent_id | bigint | Si | — |
| texto | text | No | — |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| perfil_comunidad_respuestas_parent_id_fkey | FOREIGN KEY (parent_id) REFERENCES perfil_comunidad_respuestas(id) ON DELETE CASCADE |
| perfil_comunidad_respuestas_pkey | PRIMARY KEY (id) |
| perfil_comunidad_respuestas_publicacion_id_fkey | FOREIGN KEY (publicacion_id) REFERENCES perfil_comunidad_publicaciones(id) ON DELETE CASCADE |
| perfil_comunidad_respuestas_texto_check | CHECK (((length(TRIM(BOTH FROM texto)) >= 1) AND (length(TRIM(BOTH FROM texto)) <= 800))) |
| perfil_comunidad_respuestas_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| perfil_comunidad_respuestas_parent_idx | CREATE INDEX perfil_comunidad_respuestas_parent_idx ON public.perfil_comunidad_respuestas USING btree (parent_id) |
| perfil_comunidad_respuestas_pkey | CREATE UNIQUE INDEX perfil_comunidad_respuestas_pkey ON public.perfil_comunidad_respuestas USING btree (id) |
| perfil_comunidad_respuestas_publicacion_idx | CREATE INDEX perfil_comunidad_respuestas_publicacion_idx ON public.perfil_comunidad_respuestas USING btree (publicacion_id, created_at, id) |

## public.reel_comment_likes

Filas observadas: 3. RLS: activado.

Referencias: `Backend/Controllers/reelController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| user_id | uuid | No | — |
| comment_id | bigint | No | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| reel_comment_likes_comment_id_fkey | FOREIGN KEY (comment_id) REFERENCES reel_comments(id) ON DELETE CASCADE |
| reel_comment_likes_pkey | PRIMARY KEY (user_id, comment_id) |
| reel_comment_likes_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| idx_reel_comment_likes_comment_id | CREATE INDEX idx_reel_comment_likes_comment_id ON public.reel_comment_likes USING btree (comment_id) |
| reel_comment_likes_pkey | CREATE UNIQUE INDEX reel_comment_likes_pkey ON public.reel_comment_likes USING btree (user_id, comment_id) |

## public.reel_comments

Filas observadas: 25. RLS: activado.

Referencias: `Backend/Controllers/reelController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | bigint | No | GENERATED ALWAYS AS IDENTITY |
| reel_id | bigint | No | — |
| user_id | uuid | No | — |
| parent_id | bigint | Si | — |
| texto | text | No | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |
| responde_a | text | Si | — |
| likes | integer | No | 0 |

| Restriccion | Definicion |
|---|---|
| reel_comments_parent_id_fkey | FOREIGN KEY (parent_id) REFERENCES reel_comments(id) ON DELETE CASCADE |
| reel_comments_pkey | PRIMARY KEY (id) |
| reel_comments_reel_id_fkey | FOREIGN KEY (reel_id) REFERENCES reels(id) ON DELETE CASCADE |
| reel_comments_texto_check | CHECK ((length(TRIM(BOTH FROM texto)) > 0)) |
| reel_comments_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| idx_reel_comments_parent_id | CREATE INDEX idx_reel_comments_parent_id ON public.reel_comments USING btree (parent_id) |
| idx_reel_comments_reel_id | CREATE INDEX idx_reel_comments_reel_id ON public.reel_comments USING btree (reel_id) |
| reel_comments_pkey | CREATE UNIQUE INDEX reel_comments_pkey ON public.reel_comments USING btree (id) |

## public.reel_generos

Filas observadas: 3. RLS: activado.

Referencias: `Backend/Controllers/perfilComunidadController.js`, `Backend/Controllers/reelController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| reel_id | bigint | No | — |
| genero | text | No | — |
| posicion | smallint | No | — |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| reel_generos_genero_fkey | FOREIGN KEY (genero) REFERENCES generos(slug) |
| reel_generos_pkey | PRIMARY KEY (reel_id, genero) |
| reel_generos_posicion_check | CHECK (((posicion >= 1) AND (posicion <= 3))) |
| reel_generos_reel_id_fkey | FOREIGN KEY (reel_id) REFERENCES reels(id) ON DELETE CASCADE |
| reel_generos_reel_id_posicion_key | UNIQUE (reel_id, posicion) |

| Indice | Definicion |
|---|---|
| reel_generos_genero_reel_idx | CREATE INDEX reel_generos_genero_reel_idx ON public.reel_generos USING btree (genero, reel_id) |
| reel_generos_pkey | CREATE UNIQUE INDEX reel_generos_pkey ON public.reel_generos USING btree (reel_id, genero) |
| reel_generos_reel_id_posicion_key | CREATE UNIQUE INDEX reel_generos_reel_id_posicion_key ON public.reel_generos USING btree (reel_id, posicion) |

## public.reel_likes

Filas observadas: 3. RLS: activado.

Referencias: `Backend/Controllers/reelController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| user_id | uuid | No | — |
| reel_id | bigint | No | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| reel_likes_pkey | PRIMARY KEY (user_id, reel_id) |
| reel_likes_reel_id_fkey | FOREIGN KEY (reel_id) REFERENCES reels(id) ON DELETE CASCADE |
| reel_likes_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| idx_reel_likes_reel_id | CREATE INDEX idx_reel_likes_reel_id ON public.reel_likes USING btree (reel_id) |
| reel_likes_pkey | CREATE UNIQUE INDEX reel_likes_pkey ON public.reel_likes USING btree (user_id, reel_id) |

## public.reel_playback_sessions

Filas observadas: 47. RLS: activado.

Referencias: `Backend/Controllers/reelController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | uuid | No | — |
| user_id | uuid | No | — |
| reel_id | bigint | No | — |
| listened_ms | integer | No | 0 |
| duration_ms | integer | Si | — |
| completion_ratio | numeric(5,4) | No | 0 |
| completed | boolean | No | false |
| skipped | boolean | No | false |
| replay_count | smallint | No | 0 |
| started_at | timestamp with time zone | No | timezone('utc'::text, now()) |
| updated_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| reel_playback_sessions_completion_ratio_check | CHECK (((completion_ratio >= (0)::numeric) AND (completion_ratio <= (1)::numeric))) |
| reel_playback_sessions_duration_ms_check | CHECK (((duration_ms IS NULL) OR ((duration_ms >= 1) AND (duration_ms <= 3600000)))) |
| reel_playback_sessions_listened_ms_check | CHECK (((listened_ms >= 0) AND (listened_ms <= 3600000))) |
| reel_playback_sessions_pkey | PRIMARY KEY (id) |
| reel_playback_sessions_reel_id_fkey | FOREIGN KEY (reel_id) REFERENCES reels(id) ON DELETE CASCADE |
| reel_playback_sessions_replay_count_check | CHECK (((replay_count >= 0) AND (replay_count <= 100))) |
| reel_playback_sessions_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| reel_playback_reel_idx | CREATE INDEX reel_playback_reel_idx ON public.reel_playback_sessions USING btree (reel_id, updated_at DESC) |
| reel_playback_sessions_pkey | CREATE UNIQUE INDEX reel_playback_sessions_pkey ON public.reel_playback_sessions USING btree (id) |
| reel_playback_user_recent_idx | CREATE INDEX reel_playback_user_recent_idx ON public.reel_playback_sessions USING btree (user_id, updated_at DESC) |

## public.reel_shares

Filas observadas: 1. RLS: activado.

Referencias: `Backend/Controllers/reelController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| user_id | uuid | No | — |
| reel_id | bigint | No | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| reel_shares_pkey | PRIMARY KEY (user_id, reel_id) |
| reel_shares_reel_id_fkey | FOREIGN KEY (reel_id) REFERENCES reels(id) ON DELETE CASCADE |
| reel_shares_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| idx_reel_shares_reel_id | CREATE INDEX idx_reel_shares_reel_id ON public.reel_shares USING btree (reel_id) |
| reel_shares_pkey | CREATE UNIQUE INDEX reel_shares_pkey ON public.reel_shares USING btree (user_id, reel_id) |

## public.reel_views

Filas observadas: 20. RLS: activado.

Referencias: `Backend/Controllers/reelController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| user_id | uuid | No | — |
| reel_id | bigint | No | — |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| reel_views_pkey | PRIMARY KEY (user_id, reel_id) |
| reel_views_reel_id_fkey | FOREIGN KEY (reel_id) REFERENCES reels(id) ON DELETE CASCADE |
| reel_views_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| idx_reel_views_reel_id | CREATE INDEX idx_reel_views_reel_id ON public.reel_views USING btree (reel_id) |
| reel_views_pkey | CREATE UNIQUE INDEX reel_views_pkey ON public.reel_views USING btree (user_id, reel_id) |

## public.reels

Filas observadas: 3. RLS: activado.

Referencias: `Backend/Controllers/perfilComunidadController.js`, `Backend/Controllers/reelController.js`, `Backend/Controllers/usuarioController.js`, `Backend/services/storageService.js`, `Frontend/src/componentes/CrearReelModal.jsx`, `Frontend/src/paginas/Buscar.jsx`, `Frontend/src/paginas/Comunidad.jsx`, `Frontend/src/paginas/Descubrir.jsx`, `Frontend/src/paginas/Eventos.jsx`, `Frontend/src/paginas/Miperfil.jsx`, `Frontend/src/paginas/OtroPerfil.jsx`, `Frontend/src/paginas/Soporte.jsx`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | bigint | No | GENERATED BY DEFAULT AS IDENTITY |
| titulo | text | No | — |
| genero | text | No | — |
| duracion | text | Si | — |
| portada_url | text | Si | — |
| portada_path | text | Si | — |
| audio_url | text | No | — |
| audio_path | text | No | — |
| creador_id | uuid | No | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |
| color_principal | text | Si | — |
| likes | integer | No | 0 |
| guardados | integer | No | 0 |
| compartidos | integer | No | 0 |
| visitas | integer | No | 0 |
| color_ambiente | text | No | '#8F5136'::text |
| fragment_start | numeric(8,2) | No | 0 |
| fragment_end | numeric(8,2) | Si | — |
| album | text | No | ''::text |
| descripcion | text | No | ''::text |

| Restriccion | Definicion |
|---|---|
| reels_color_principal_formato_check | CHECK (((color_principal IS NULL) OR (color_principal ~ '^#[0-9a-fA-F]{6}$'::text))) |
| reels_creador_id_fkey | FOREIGN KEY (creador_id) REFERENCES users(id) ON DELETE CASCADE |
| reels_pkey | PRIMARY KEY (id) |

| Indice | Definicion |
|---|---|
| reels_creador_id_idx | CREATE INDEX reels_creador_id_idx ON public.reels USING btree (creador_id) |
| reels_created_at_idx | CREATE INDEX reels_created_at_idx ON public.reels USING btree (created_at DESC) |
| reels_pkey | CREATE UNIQUE INDEX reels_pkey ON public.reels USING btree (id) |

## public.user_blocks

Filas observadas: 0. RLS: activado.

Referencias: `Backend/Controllers/eventoController.js`, `Backend/Controllers/mensajeController.js`, `Backend/Controllers/perfilComunidadController.js`, `Backend/Controllers/reelController.js`, `Backend/Controllers/usuarioController.js`, `Backend/services/moderationService.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| blocker_id | uuid | No | — |
| blocked_id | uuid | No | — |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| user_blocks_blocked_id_fkey | FOREIGN KEY (blocked_id) REFERENCES users(id) ON DELETE CASCADE |
| user_blocks_blocker_id_fkey | FOREIGN KEY (blocker_id) REFERENCES users(id) ON DELETE CASCADE |
| user_blocks_different_users | CHECK ((blocker_id <> blocked_id)) |
| user_blocks_pkey | PRIMARY KEY (blocker_id, blocked_id) |

| Indice | Definicion |
|---|---|
| idx_user_blocks_blocked_id | CREATE INDEX idx_user_blocks_blocked_id ON public.user_blocks USING btree (blocked_id) |
| user_blocks_pkey | CREATE UNIQUE INDEX user_blocks_pkey ON public.user_blocks USING btree (blocker_id, blocked_id) |

## public.user_genre_affinity

Filas observadas: 11. RLS: activado.

Referencias: `Backend/Controllers/reelController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| user_id | uuid | No | — |
| genre | text | No | — |
| behavioral_score | numeric(10,3) | No | 0 |
| listening_sessions | integer | No | 0 |
| last_interaction_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| user_genre_affinity_pkey | PRIMARY KEY (user_id, genre) |
| user_genre_affinity_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| user_genre_affinity_pkey | CREATE UNIQUE INDEX user_genre_affinity_pkey ON public.user_genre_affinity USING btree (user_id, genre) |
| user_genre_affinity_score_idx | CREATE INDEX user_genre_affinity_score_idx ON public.user_genre_affinity USING btree (user_id, behavioral_score DESC) |

## public.user_interests

Filas observadas: 16. RLS: activado.

Referencias: `Backend/Controllers/eventoController.js`, `Backend/Controllers/reelController.js`, `Backend/Controllers/usuarioController.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| user_id | uuid | No | — |
| genre | text | No | — |
| created_at | timestamp with time zone | No | timezone('utc'::text, now()) |

| Restriccion | Definicion |
|---|---|
| user_interests_genre_check | CHECK ((genre = ANY (ARRAY['pop'::text, 'rock'::text, 'trap'::text, 'cumbia'::text, 'edm'::text, 'jazz'::text, 'blues'::text, 'metal'::text, 'folklore'::text, 'otros'::text]))) |
| user_interests_pkey | PRIMARY KEY (user_id, genre) |
| user_interests_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| user_interests_genre_idx | CREATE INDEX user_interests_genre_idx ON public.user_interests USING btree (genre, user_id) |
| user_interests_pkey | CREATE UNIQUE INDEX user_interests_pkey ON public.user_interests USING btree (user_id, genre) |

## public.user_settings

Filas observadas: 0. RLS: activado.

Referencias: `Backend/Controllers/usuarioController.js`, `Backend/services/notificationService.js`, `Backend/services/settingsSchema.js`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| user_id | uuid | No | — |
| telefono | text | No | ''::text |
| idioma | text | No | 'es'::text |
| actividad_cuenta | boolean | No | true |
| mostrar_email | boolean | No | false |
| updated_at | timestamp with time zone | No | timezone('utc'::text, now()) |
| codigo_pais | text | No | '+54'::text |
| notificar_interacciones | boolean | No | true |
| notificar_comentarios | boolean | No | true |
| notificar_seguidores | boolean | No | true |
| notificar_publicaciones | boolean | No | true |
| notificar_menciones | boolean | No | true |
| reducir_movimiento | boolean | No | false |
| notificar_mensajes | boolean | No | true |

| Restriccion | Definicion |
|---|---|
| user_settings_codigo_pais_check | CHECK ((codigo_pais = ANY (ARRAY['+54'::text, '+55'::text, '+56'::text, '+598'::text]))) |
| user_settings_idioma_check | CHECK ((idioma = ANY (ARRAY['es'::text, 'en'::text, 'pt'::text]))) |
| user_settings_pkey | PRIMARY KEY (user_id) |
| user_settings_telefono_check | CHECK ((length(telefono) <= 30)) |
| user_settings_user_id_fkey | FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE |

| Indice | Definicion |
|---|---|
| user_settings_pkey | CREATE UNIQUE INDEX user_settings_pkey ON public.user_settings USING btree (user_id) |

## public.users

Filas observadas: 9. RLS: activado.

Referencias: `Backend/Controllers/comunidadController.js`, `Backend/Controllers/eventoController.js`, `Backend/Controllers/mensajeController.js`, `Backend/Controllers/notificacionController.js`, `Backend/Controllers/perfilComunidadController.js`, `Backend/Controllers/reelController.js`, `Backend/Controllers/usuarioController.js`, `Backend/services/moderationService.js`, `Backend/services/notificationService.js`, `Backend/services/settingsSchema.js`, `Frontend/src/contextos/PreferenciasContext.jsx`.

| Columna | Tipo exacto | Permite NULL | Default / identity |
|---|---|---|---|
| id | uuid | No | — |
| email | text | No | — |
| username | text | No | — |
| created_at | timestamp with time zone | Si | timezone('utc'::text, now()) |
| profile_img_url | text | Si | — |
| bio | text | No | ''::text |
| updated_at | timestamp with time zone | Si | — |
| profile_img_path | text | Si | — |
| display_name | text | Si | — |
| birth_date | date | Si | — |
| user_type | text | No | 'musico'::text |
| full_name | text | Si | — |
| artist_name | text | Si | — |
| artist_bio | text | Si | — |
| banner_url | text | Si | — |
| instagram_url | text | Si | — |
| verified | boolean | Si | false |
| onboarding_completed | boolean | No | false |

| Restriccion | Definicion |
|---|---|
| users_birth_date_min_check | CHECK (((birth_date IS NULL) OR (birth_date >= '1900-01-01'::date))) |
| users_email_key | UNIQUE (email) |
| users_id_fkey | FOREIGN KEY (id) REFERENCES auth.users(id) ON DELETE CASCADE |
| users_pkey | PRIMARY KEY (id) |
| users_user_type_check | CHECK ((user_type = ANY (ARRAY['musico'::text, 'organizador'::text, 'admin'::text]))) |
| users_username_formato_check | CHECK ((username ~ '^[a-z0-9._-]{3,30}$'::text)) |
| users_username_key | UNIQUE (username) |

| Indice | Definicion |
|---|---|
| users_email_key | CREATE UNIQUE INDEX users_email_key ON public.users USING btree (email) |
| users_pkey | CREATE UNIQUE INDEX users_pkey ON public.users USING btree (id) |
| users_username_key | CREATE UNIQUE INDEX users_username_key ON public.users USING btree (username) |
| users_username_lower_unique | CREATE UNIQUE INDEX users_username_lower_unique ON public.users USING btree (lower(username)) |

## Funciones, triggers y seguridad

Definiciones completas en `01_instalacion_compatible.sql`; metadatos originales en `catalogo-observado.json`.

| Funcion |
|---|
| actualizar_afinidad_desde_escucha() |
| actualizar_conversacion_por_mensaje() |
| broadcast_conversation_read_change() |
| broadcast_message_change() |
| crear_publicacion_automatica_perfil() |
| is_conversation_member(uuid,uuid) |
| playback_signal_score(integer,numeric,boolean,boolean,smallint) |
| sincronizar_ubicacion_evento() |
| sync_auth_user_email() |
| validar_padre_respuesta_perfil() |

| Trigger | Tabla |
|---|---|
| on_auth_user_email_updated | auth.users |
| conversation_members_broadcast_read | public.conversation_members |
| eventos_sincronizar_ubicacion | public.eventos |
| perfil_comunidad_evento_creado | public.eventos |
| messages_actualizar_conversacion | public.messages |
| messages_broadcast_change | public.messages |
| perfil_comunidad_respuesta_padre_valido | public.perfil_comunidad_respuestas |
| reel_playback_actualizar_afinidad | public.reel_playback_sessions |
| perfil_comunidad_reel_creado | public.reels |
